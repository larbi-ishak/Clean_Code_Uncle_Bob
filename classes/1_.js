/**
 * why create classes
 * reduce complexity
 * more cohesion between methods and variables
 *  DRY
 *
 */

/**
 * probably if you provide same variable(s) to many functions , those should be a class with methods
 *
 *
 */

/**
 * Cohesive
 *
 * watch out for : methods doesn't interact with the rest of others
 *
 * Low cohesive classes: their classes doesn't interact much with the rest of others
 */

/**
 * sometimes , you can find rare mistakes like if you have two small classes related heavily on each other
 * those should be merged in one more big class
 */

/**
 * like functions , many parameters for class combine them in one object
 */
